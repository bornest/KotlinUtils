KotlinUtils-concurrent
======================

Thread factories, extensions & utility functions for Atomic types, Threads, Executors and Locks.  

Description
-----------

This module has 2 main packages: `android` and `java`:

- `java` package contains platform-independent code
- `android` package contains Android-specific code

Both packages have similar structure.
